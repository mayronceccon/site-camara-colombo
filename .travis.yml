language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm test
  - npm run build
notifications:
  email:
    recipients:
      - mayron.ceccon@gmail.com
    on_success: always
    on_failure: always
addons:
  apt:
    packages:
      - sshpass
after_success:
  - export SSHPASS=${pass_scp}
  - sshpass -e scp -r ./build/* ${user_scp}@${server_scp}:/home/dados-colombo/teste